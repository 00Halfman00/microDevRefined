# Next.js build output
.next/
out/ # If you use 'next export'
.next-build/ # Sometimes generated during build

# node_modules
node_modules/

# Next.js cache directory
.next/cache/

# Environment variables
.env.local
.env.development.local
.env.production.local
.env.test.local

# npm/yarn lock files (choose one based on what you use)
# package-lock.json (if you typically don't commit it, but it's often committed)
# yarn.lock (often committed)
# pnpm-lock.yaml (often committed)

# Mac and OS-specific files
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# VS Code directory (optional, if you don't want to share settings)
.vscode/

# Editor directories and files
.idea/
.project
.classpath
*.sublime-project
*.sublime-workspace

# Ignore the common library directory to treat it as a separate Git repo.
/common/
